#include <stdio.h>

int main() {
    char ch;

    //Taking character input
    printf("Enter a character: ");
    scanf("%c", &ch);

    //Printing the ASCII value
    printf("ASCII value: %d\n", ch);

      return 0;
}