#include <stdio.h>

int main() {
    char ch = 'a';
    printf("The character is %c\n", ch);
    printf("The value of character is %d\n", ch);
    // 65, z
    if(ch>=65 && ch = z){
        printf("ASCII Value: \n");
    }
    return 0;
}