#include <stdio.h>

int main() {
    char str[100]; //Declaring a character array to store the string

    //Taking string input(Single word)
    scanf("%s", str);

    //Printing the entered string
    printf("You entered: %s\n", str);

    return 0;
}