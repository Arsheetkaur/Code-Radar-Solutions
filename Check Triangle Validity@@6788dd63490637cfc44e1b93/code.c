// Your code here...
// a + b > c
// b + c > a
// c  + a > b
# include <stdio.h>

int main() {
    int num1, num2, num3;
    scanf("%d %d %d", &num1, &num2, &num3);
    if (a + b > c && b + c > a && c + a > b) {
        printf("Valid \n");
    }
    else {
        printf("Invalid");
    }
    return 0;
}